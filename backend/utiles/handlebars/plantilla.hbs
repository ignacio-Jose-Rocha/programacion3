<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8">
    <title>Informe de Reclamos</title>
    <style>
        body { font-family: Arial, sans-serif; margin: 20px; }
        .header { text-align: center; margin-bottom: 30px; }
        .title { color: #333; font-size: 24px; margin-bottom: 10px; }
        .date { color: #666; font-size: 14px; }
        table { width: 100%; border-collapse: collapse; margin-top: 20px; }
        th, td { border: 1px solid #ddd; padding: 8px; text-align: left; }
        th { background-color: #f2f2f2; font-weight: bold; }
        tr:nth-child(even) { background-color: #f9f9f9; }
        .footer { margin-top: 30px; text-align: center; font-size: 12px; color: #666; }
    </style>
</head>
<body>
    <div class="header">
        <h1 class="title">Informe de Reclamos</h1>
        <p class="date">Generado el: {{fecha}}</p>
    </div>
    
    <table>
        <thead>
            <tr>
                <th>Reclamo</th>
                <th>Tipo</th>
                <th>Estado</th>
                <th>Fecha Creado</th>
                <th>Cliente</th>
            </tr>
        </thead>
        <tbody>
            {{#each reclamos}}
            <tr>
                <td>{{this.reclamo}}</td>
                <td>{{this.tipo}}</td>
                <td>{{this.estado}}</td>
                <td>{{this.fechaCreado}}</td>
                <td>{{this.cliente}}</td>
            </tr>
            {{/each}}
        </tbody>
    </table>
    
    <div class="footer">
        <p>Concesionaria Prog.III - Sistema de Gestión de Reclamos</p>
    </div>
</body>
</html>